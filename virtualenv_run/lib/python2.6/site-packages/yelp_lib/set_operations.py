# -*- coding: utf-8 -*-
"""
Utility functions for doing basic set theory operations.
"""

import itertools


# Backwards compatibility, for now
combinations = itertools.combinations
permutations = itertools.permutations


# Rip of powerset from python doc examples
# see http://docs.python.org/library/itertools.html
def powerset(iterable):
    """
    Returns the powerset of the iterable.

    ppowerset([1,2,3]) --> () (1,) (2,) (3,) (1,2) (1,3) (2,3) (1,2,3)
    """
    s = list(iterable)
    return itertools.chain.from_iterable(
        combinations(s, r) for r in xrange(len(s)+1)
    )
