"""
Utility methods for manipulating strings.
"""

def get_unicode(s, encoding=None):
    """Safe unicode converter. Given a basestring , returns a unicode object or None."""
    try:
        if encoding is not None:
            return unicode(s, encoding=encoding)
        else:
            return unicode(s)
    except UnicodeDecodeError:
        return None
