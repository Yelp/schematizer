import os.path

def path(*subpaths):
    """Get the path of the datafile directory or a path of a datafile."""
    if subpaths:
        return os.path.join(os.path.dirname(__file__), *subpaths)
    else:
        return os.path.dirname(__file__)
