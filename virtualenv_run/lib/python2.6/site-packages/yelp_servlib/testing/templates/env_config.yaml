

module_env_config:
    -   namespace: clog
        initialize: yelp_servlib.clog_util.initialize
        config:
            log_stream_name: testing_app_log
            log_dir: {clog_log_dir}

    -   namespace: yelp_conn
        file: /nail/srv/configs/yelp_conn_generic.yaml
        config:
            connection_set_file: 'connection_sets.yaml'
            topology: {topology_filename}
